@model IEnumerable<MvcApplication8.Models.Tableta>

@{
    ViewBag.Title = "tabletaeliminar";
}
<section class="p10"></section>
<section class="container form-horizontal ">
    <div class="col-sm-11 col-sm-offset-1">
        <div class="form-group ">
            <div class="text-left ">
                <h1>Tabletas</h1>
                <h2>Tabletas Registradas</h2>
            </div>
        </div>
        <div class="form-group">
            <div class="form-group ">
                <div class="col-sm-3 col-sm-offset-1">
                    <button type="submit" class="btn boton btnPrimary btn-lg btn-block" onclick="location.href='@Url.Action("DownloadListatableta", "Proveedor")';"><span class="glyphicon glyphicon-save"></span> Descargar Lista</button>
                </div>
            </div>

            <div class="form-group">

                <div class="col-sm-10 col-sm-offset-1">
                    <table width=100% class="table  table-hover table-condensed table-striped" id="tabladatos" align="left">
                        <thead>
                            <tr class="bakinfo">
                                <th width="5%">
                                    @Html.DisplayNameFor(model => model.No)
                                </th>
                                <th width="30%">
                                    @Html.DisplayNameFor(model => model.serie)
                                </th>

                                <td width="35%">
                                    <h4>Intitución</h4>

                                </td>
                                <th "25%">
                                    @Html.DisplayNameFor(model => model.estado)
                                </th>
                                <th width="5%">Eliminar</th>
                            </tr>

                        </thead>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.No)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.serie)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.nombre)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.estado)
                                </td>
                                <td></td>
                            </tr>
                        }

                    </table>
                </div>
            </div>
</section>